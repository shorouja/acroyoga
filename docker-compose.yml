services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: acroyoga
      POSTGRES_USER: acro_user
      POSTGRES_PASSWORD: local_dev_password
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
