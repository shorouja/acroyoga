# Maintenance mode: hides the app behind a static page (only Impressum /
# Datenschutz stay reachable). Vite loads this file only during `vite build`
# (production). Set to false and redeploy to bring the full app back.
VITE_MAINTENANCE=true
